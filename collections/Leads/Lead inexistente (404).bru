meta {
  name: Lead inexistente (404)
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/tenants/{{tenantId}}/leads/00000000-0000-0000-0000-000000000000
  body: none
  auth: none
}

tests {
  test("status code é 404", function() {
    expect(res.getStatus()).to.equal(404);
  });

  test("retorna erro NotFoundError", function() {
    const body = res.getBody();
    expect(body).to.have.property("error");
    expect(body.error.code).to.equal("NotFoundError");
  });
}

docs {
  # Lead inexistente (404)

  Busca um lead com UUID válido mas que não existe no tenant.

  ## Resposta (404)

  ```json
  {
    "error": {
      "code": "NotFoundError",
      "message": "Lead not found"
    }
  }
  ```
}
