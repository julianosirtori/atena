meta {
  name: Deletar agent
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/v1/tenants/{{tenantId}}/agents/{{agentId}}
  body: none
  auth: none
}

tests {
  test("status code é 204", function() {
    expect(res.getStatus()).to.equal(204);
  });
}

docs {
  # Deletar agent

  Remove um agente do tenant.

  ## Path params

  | Param | Tipo | Descrição |
  |-------|------|-----------|
  | `tenantId` | UUID | ID do tenant |
  | `agentId` | UUID | ID do agente |

  ## Resposta (204)

  Sem corpo — retorna `204 No Content`.

  ## Erros

  | Código | Descrição |
  |--------|-----------|
  | `404` | Agente não encontrado |
}
